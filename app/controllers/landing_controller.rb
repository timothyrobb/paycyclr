class LandingController < ApplicationController
  def index
    # Welcome page, displaying features and login link
    # Perhaps loading of session, to detect if logged in and redirect to dashboard
  end
end
